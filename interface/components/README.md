# Shared Components (Planned)

This directory will contain device cards, players, timelines, clip lists, and job progress cards shared by fixed pages and the Agent workspace. The current page has HTML/CSS and a separate JavaScript module; this directory does not contain an implemented component library. Components must use the shared [i18n catalogs and language preference](../i18n/README.md), including labels for structured results.
